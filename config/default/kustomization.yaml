apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace where the operator will be deployed
namespace: mock-storage-operator-system

resources:
  - ../rbac
  - ../manager

# Images to use
images:
  - name: mock-storage-operator
    newName: quay.io/bmekhiss/mock-storage-operator
    newTag: latest

# Made with Bob
